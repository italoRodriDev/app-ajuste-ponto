import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ajuste de Ponto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
